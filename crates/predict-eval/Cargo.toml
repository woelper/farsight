[package]
name = "predict-eval"
version = "0.1.0"
edition = "2021"

[dependencies]
predict-core = { path = "../predict-core" }
predict-llm = { path = "../predict-llm" }
thiserror = "2"

[dev-dependencies]
predict-ngram = { path = "../predict-ngram" }
predict-store = { path = "../predict-store" }

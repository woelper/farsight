[package]
name = "predict-llm"
version = "0.1.0"
edition = "2021"

[dependencies]
predict-core = { path = "../predict-core" }
thiserror = "2"
serde = { version = "1", features = ["derive"] }
toml = "1"
llama-cpp-2 = "0.1"

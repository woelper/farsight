[package]
name = "predict-store"
version = "0.1.0"
edition = "2021"

[dependencies]
predict-core = { path = "../predict-core" }
predict-ngram = { path = "../predict-ngram" }
rusqlite = { version = "0.40", features = ["bundled"] }
thiserror = "2"
